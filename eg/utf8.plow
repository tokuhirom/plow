unshift @INC, 'eg/lib';

warn utf8::is_utf8('あいう');
warn 'あいう';
require Hoge;

my $src = file('eg/utf8.plow')->slurp(iomode => '<:utf8');
warn utf8::is_utf8($src);
my $src2 = file('eg/utf8.plow')->slurp();
warn utf8::is_utf8($src2);
my $src3 = file('eg/utf8.plow')->slurp(iomode => '<:raw');
warn utf8::is_utf8($src3);

p([[[1,3,3,],4,44,],452]);

func foo() {
    say "HO";
}

foo();
